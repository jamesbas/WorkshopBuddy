# Build artifacts
node_modules
.next
out
dist

# VCS / IDE
.git
.gitignore
.vscode
.idea

# Local env / data
.env
.env.*
!.env.example
data/*.db
*.log

# Docker / compose (not needed inside the image)
Dockerfile
.dockerignore
docker-compose*.yml

# azd state
.azure

# Tactical temp files written by deploy scripts (prevents `tar: write too long`)
scripts/utils/_temp_*

# Docs & samples (kept out of the container)
docs
*.md
!README.md
